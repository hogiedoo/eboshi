- content_for :title do
  =h @client.name
  &raquo; Editing Time Item

= error_messages_for :work

- form_for @work do |f|
  = render :partial => 'form', :object => f, :as => :f
  %p= f.submit "Update"

= link_to 'Back', invoices_path(@client)

= button_to "Convert to Flat fee", convert_work_path(@work)
